# Copy to .env and fill in values from your EVE developer app at
# https://developers.eveonline.com  (callback URL must match what is registered).
EVE_CLIENT_ID=
EVE_CLIENT_SECRET=
EVE_CALLBACK_URL=
# Random string used for session encryption. Generate with: openssl rand -hex 32
SECRET_KEY=
